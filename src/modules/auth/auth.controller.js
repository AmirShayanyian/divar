class AuthController {
  #service;
  // constructor(

  // )

  static async sendOTP(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }

  static async checkOTP(req, res, next) {
    try {
    } catch (error) {
      next(error);
    }
  }
}
